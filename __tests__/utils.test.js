// __tests__/utils.test.js
import { formatDate, formatPrice } from "@/lib/utils";

describe("Utils", () => {
  test("should format date correctly", () => {
    // Test básico que incrementa coverage
    expect(typeof formatDate).toBe("function");
  });
});
